# soft global overrides
[ -z "$DOCKER_CONFIG" ] && export DOCKER_CONFIG=<%= @configdir %>
[ -z "$MACHINE_STORAGE_PATH" ] && export MACHINE_STORAGE_PATH=<%= @datadir %>

eval "$(docker-machine env <%= @machinename %> 2>/dev/null)"

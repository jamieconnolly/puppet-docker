# Docker config vars

export BOXEN_DOCKER_DIR=<%= @datadir %>
export BOXEN_DOCKER_PROFILE=<%= @configdir %>/profile
export BOXEN_DOCKER_HOST=tcp://192.168.59.103:2375

# soft global overrides
[ -z "$BOOT2DOCKER_DIR" ] && export BOOT2DOCKER_DIR=$BOXEN_DOCKER_DIR
[ -z "$BOOT2DOCKER_PROFILE" ] && export BOOT2DOCKER_PROFILE=$BOXEN_DOCKER_PROFILE
[ -z "$DOCKER_HOST" ] && export DOCKER_HOST=$BOXEN_DOCKER_HOST

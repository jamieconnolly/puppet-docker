---
docker::configdir:  "%{::boxen::config::configdir}/docker"
docker::datadir:    "%{::boxen::config::datadir}/docker"
docker::executable: "%{::boxen::config::homebrewdir}/bin/boot2docker"
docker::logdir:     "%{::boxen::config::logdir}/docker"
docker::user:       "%{::boxen_user}"

docker::package:    'boxen/brews/docker'
docker::version:    '1.2.0-boxen1'

docker::service:    'dev.docker'
